% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf_poppler.R
\name{clean_with_poppler}
\alias{clean_with_poppler}
\title{Extract text from a PDF with Poppler}
\usage{
clean_with_poppler(pdf_path, pdftotext = "pdftotext", quiet = TRUE)
}
\arguments{
\item{pdf_path}{Path to the PDF file.}

\item{pdftotext}{Executable name or full path to `pdftotext`
(default `"pdftotext"` must be on `PATH`).}

\item{quiet}{Logical. If `TRUE`, silences Poppler warnings by sending
`stderr` to `FALSE`.}
}
\value{
`character(1)`, page text concatenated with `\\n`.
}
\description{
Lightweight wrapper for **`pdftotext`** (Poppler). Returns the entire PDF as
a single UTF-8 string, preserving multi-column layout with `-layout`.
}
\details{
**Poppler requirement**
  * **Windows**: OSchwartz build or *Chocolatey* `poppler`
  * **macOS** : `brew install poppler`
  * **Linux** : `sudo apt-get install poppler-utils`
}
\examples{
\dontrun{
pdf <- system.file("extdata", "example.pdf", package = "yourpackage")
txt <- clean_with_poppler(pdf)
writeLines(txt)
}
}
