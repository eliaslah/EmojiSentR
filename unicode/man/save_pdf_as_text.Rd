% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf_poppler.R
\name{save_pdf_as_text}
\alias{save_pdf_as_text}
\title{Save a PDF as plain text (via Poppler)}
\usage{
save_pdf_as_text(
  pdf_path,
  out_path = NULL,
  use_layout = TRUE,
  pdftotext = "pdftotext",
  quiet = TRUE
)
}
\arguments{
\item{pdf_path}{Path to the PDF file.}

\item{out_path}{Destination `.txt`.  Default: same name as PDF.}

\item{use_layout}{Keep multi-column spacing with `-layout`. Set `FALSE` for
linear reading order (`pdftotext`’s default).}

\item{pdftotext}{Executable name or full path to `pdftotext`
(default `"pdftotext"` must be on `PATH`).}

\item{quiet}{Logical. If `TRUE`, silences Poppler warnings by sending
`stderr` to `FALSE`.}
}
\value{
(Invisibly) the path to the written text file.
}
\description{
Converts *pdf_path* to UTF-8 text and writes it to *out_path*.
}
\examples{
\dontrun{
pdf <- system.file("extdata", "example.pdf", package = "yourpackage")
save_pdf_as_text(pdf)                   # writes example.txt
}
}
